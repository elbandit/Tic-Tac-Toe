SpecFlow
In order to create new SpecFlow features you will need to install the specflow templates. Run the SpecFlow installation program found in the tools folder.

TestDriven.NET
The easiest way to run the tests is by installing the TestDriven.NET test runner which can be found in the tools folder. This will add an option to the context sensative menu when you right click on the Spec projects.